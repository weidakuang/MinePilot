# Rebuild backup checkpoint

The user requested a separate GitHub backup branch on September 8, 2026:
`codex/backup-rebuild-20260908`, based on local clean-rebuild commit `467afa7`.
Existing remote branches are not replaced or force-pushed. This preserves the
rebuild source, Skill, resources, tests and dated review records before mining
implementation begins. It is not a release or new gameplay acceptance.

Fresh pre-backup validation: 25 JUnit tests (zero failures/errors/skips), 46 Python
tests, staged-content credential/path scan, JSON parsing, Python syntax parsing,
and the repository pre-commit gate. Existing physical evidence and failures are
described in the September 7 movement continuation report. Physical trials were
not rerun for this backup.

No world saves, runtime credentials, generated JARs, Gradle caches or raw server
logs are included. Existing local evidence and installed JAR backups are retained.
The installed artifact hash remains recorded in the installation JSON; a Git
source backup is not a fresh JAR installation or client acceptance.

The original parkour remains failed and ordinary-language stop latency remains
unacceptable for immediate stopping. Mining and collection orchestration is the
next design topic, not an implemented capability in this checkpoint.
