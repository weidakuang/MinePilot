package dev.mcai.companion.agent.knowledge;

/** Private provenance metadata, serialized separately from native container contents. */
public interface StoredOrigins {
    String minepilot$getOrigins();
    void minepilot$setOrigins(String value);
}
